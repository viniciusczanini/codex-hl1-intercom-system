# Final Objective Reached Implementation Plan

**Goal:** Change `task_complete` to “Final objective reached.”

1. Add a failing manifest regression test for `final`, `objective`, `reached`.
2. Add the authentic `vox/reached.wav` fragment and update the phrase.
3. Rebuild bundled assets and update documentation.
4. Run the complete suite, commit, and push `main`.
