# Final Objective Reached Phrase Design

Replace only the `task_complete` spoken phrase with `Final objective reached.` using the authentic VOX fragments `final.wav`, `objective.wav`, and `reached.wav`. The existing `buzwarn.wav` prefix remains automatic. Queue-item and queue-complete phrases do not change.

The manifest, bundled WAV, English documentation, and phrase regression test must agree. Normal installation continues using the bundled asset without downloading sounds.
